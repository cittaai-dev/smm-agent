You are drafting the "{{ section_label }}" section of a Market Research document.

{% include "partials/domain_knowledge.jinja" %}

## Evidence pairs
Each pair contrasts what was observed for this brand against a Market Intel Core benchmark for the
same or a comparable metric.
{% include "partials/bridge_block.jinja" %}

## Task
Write claims that compare the observed value against its benchmark using ONLY the evidence pairs above.
Every claim must cite BOTH the `chunk_id` of the `<observed>` tag (as chunk_id) AND the `chunk_id` of the
`<benchmark>` tag (as supporting_chunk_id) it came from. Never cite a benchmark without its paired
observation, or vice versa.

If the evidence pairs above are insufficient to write this section, output an empty claims list rather
than inventing a comparison. An empty, honest section beats a fabricated one.

{% if structured_fields %}
## This section renders as a table, not a paragraph
Every claim you write is one cell of a table. In addition to the usual citation:
- group_key: the {{ structured_group_label }}'s name this claim is about.
{% if structured_row_values %}
  Must be exactly one of: {{ structured_row_values | join(", ") }}. Do not invent a
  {{ structured_group_label }} outside this list.
{% endif %}
- field_key: which column this claim fills. Must be exactly one of: {{ structured_fields | join(", ") }}.

Write at most one claim per (group_key, field_key) pair. If the evidence doesn't support a given cell,
simply don't write a claim for it -- an empty cell is honest; a fabricated one is not.
{% endif %}

## Output
Return a list of claims, each with: section ("{{ section_id }}"), text, chunk_id, supporting_chunk_id{% if structured_fields %}, group_key, field_key{% endif %}.
